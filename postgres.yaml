volumePermissions:
  enabled: true
primary:
  podLabels:
    app.kubernetes.io/managed-by: vkpr
  persistence:
    size: 8Gi
fullnameOverride: postgres-postgresql
global:
  postgresql:
    auth:
      postgresPassword: vkpr123
      database: kong